apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tutorial-pvc
  namespace: tutorial
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

